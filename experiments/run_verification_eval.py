"""Stage-4 impact -> ``results/verification_eval.md``.

Measures the share of Stage-3 outputs containing at least one hallucinated quantity
or unsafe item, before versus after verification, for the template provider and
(when keys are present) each LLM provider.

TODO(Phase 4): implement.
"""
