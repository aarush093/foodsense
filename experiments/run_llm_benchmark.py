"""Provider comparison -> ``results/llm_benchmark.md``.

Macro RMSE against USDA ground truth, goal consistency and output diversity across
Template / Anthropic / OpenAI / Ollama. Skips any provider whose credentials are
absent, and says so in the report rather than inventing a row.

TODO(Phase 6): implement.
"""
